.info-collection {
  position: relative;
  height: 100%;
  .info-collection-refresh {
    position: absolute;
    top: px2rem(100);
    bottom: 0;
  }
  .text-center {
    text-align: center;
  }
  .loading {
      position: absolute;
      left: 50%;
      margin-left: -32px;
      top: 50%;
      margin-top: -32px;
      z-index: 9999;
  }
}