import { reducer as coursewareAudioEditerReducer } from '../modules/courseware-audio-editer/reducer'

const finalReducer = {
  coursewareAudioEditer: coursewareAudioEditerReducer
}

export default finalReducer
